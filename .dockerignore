# Keep build context small and avoid leaking local secrets into the image layer cache.
.git
.github
**/.git

# Dependencies (installed fresh in Dockerfile)
**/node_modules

# Build outputs
**/dist
**/build
web-build

# Apps not in Docker web image
mobile
functions

# Local env and secrets (never copy into image)
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# Misc
*.log
.DS_Store
Thumbs.db
.expo
.firebase
